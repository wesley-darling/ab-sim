def test_business_event():
    # mem = MemorySink()
    # rec = Recorder(mem)
    # hooks = KernelJSONHooks(run_id="test", recorder=rec, level="ERROR")  # quiet logs
    # k = Kernel(hooks=hooks)
    # # … run the sim …
    # trip_completed = [e for e in mem.events if e.name == "TripCompleted"]
    # assert trip_completed and trip_completed[0].rider_id == 42
    pass
